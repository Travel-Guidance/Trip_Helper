# Leave empty in local development so Vite proxies /api to http://localhost:3001.
# In production set this to the backend origin only. Do not include /api.
VITE_API_BASE=

# Optional WebSocket backend origin for deployed collaboration pages.
# Leave empty locally.
VITE_WS_BASE=
